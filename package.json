{
	"name": "planet-shooter",
	"type": "module",
	"scripts": {
		"build": "vite build",
		"dev": "vite",
		"preview": "vite preview",
		"zip": "npm run build && mkdir -p dist && zip -r dist/game.zip www -x \"**/.DS_Store\"",
		"check": "tsc"
	},
	"dependencies": {
		"kaplay": "^4000.0.0-alpha.21"
	},
	"devDependencies": {
		"@types/node": "^22.7.5",
		"typescript": "^5.6.3",
		"vite": "^7.1.9"
	}
}
